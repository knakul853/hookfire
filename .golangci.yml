version: "2"
run:
  timeout: 3m
linters:
  # errcheck, govet, ineffassign, staticcheck, and unused are enabled by default in v2.
  enable:
    - revive
    - misspell
    - unconvert
    - bodyclose
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
