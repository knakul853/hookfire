run:
  timeout: 3m
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - misspell
    - unconvert
    - bodyclose
issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
